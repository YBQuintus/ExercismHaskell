name: dnd-character
version: 1.1.0.1

dependencies:
  - base
  - QuickCheck

library:
  exposed-modules: DND
  source-dirs: src
  ghc-options: -Wall
  # dependencies:

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - dnd-character
      - hspec
